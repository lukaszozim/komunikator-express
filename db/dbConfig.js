const Sequelize = require("sequelize");

config = {
    database: "komunikator",
    username: "PHP",
    password: "mysql",
    host: "127.0.0.1",
    dialect:'mysql'
  };

  module.exports = config;